on:
  push:
    branches:
      - 'main'
  workflow_dispatch:

jobs:
  develop:
    uses: ./.github/workflows/REUSABLE_WORKFLOW_FILE.yml
    with:
      image_name: my-awesome-app
      tag: ${{ github.run_number }}
    
  output:
    runs-on: ubuntu
    depends: build
    steps:
      - name: call ouput
        env:
          DOCKER_IMAGE: ${{ needs.build.outputs.docker-build-image-name }}
        run: echo $DOCKER_IMAGE
